# Telegram Communication Analysis Bot

Телеграм-бот для анализа коммуникационных паттернов в групповых чатах с использованием AI. Бот мониторит сообщения в чатах, кеширует их в памяти и предоставляет AI-анализ по запросу авторизованных пользователей.

## 🚀 Возможности

- **Анализ групповых коммуникаций**: Анализ последних сообщений или за определенный период
- **Персональный анализ**: Анализ стиля общения конкретного пользователя
- **Кросс-чатовый анализ**: Анализ пользователя сразу из всех чатов с ботом
- **Система авторизации**: Ограничение доступа к командам для определенных пользователей
- **Управление пользователями**: Добавление/удаление авторизованных пользователей
- **Статистика чатов**: Просмотр статистики кешированных сообщений
- **Защита от спама**: Ограничение частоты выполнения команд
- **Оптимизация памяти**: Умное кеширование с минимальным потреблением RAM

## 📋 Требования

- Python 3.11 или новее
- uv (современный менеджер пакетов Python) или pip
- Telegram Bot Token (от @BotFather)
- OpenAI API Key

## 🛠️ Установка

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd telegram-communication-bot
```

### 2. Установка зависимостей

#### Используя uv (рекомендуется):

```bash
# Установка uv
curl -LsSf https://astral.sh/uv/install.sh | sh

# Установка зависимостей
uv sync
```

#### Используя pip:

```bash
# Создание виртуального окружения
python -m venv venv

# Активация окружения
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Установка зависимостей
pip install aiogram>=3.22.0 openai>=1.101.0 python-dotenv>=1.1.1
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env  # если есть пример
# или создайте новый файл .env
```

Добавьте следующие переменные в `.env`:

```env
# Обязательные переменные
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
AUTHORIZED_USERS=123456789,987654321

# Опциональные переменные (с значениями по умолчанию)
CACHE_SIZE=1000
RATE_LIMIT_SECONDS=10
LOG_LEVEL=INFO
```

### 4. Получение необходимых токенов

#### Telegram Bot Token:
1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

#### OpenAI API Key:
1. Зайдите на [platform.openai.com](https://platform.openai.com)
2. Создайте аккаунт или войдите
3. Перейдите в раздел API Keys
4. Создайте новый API ключ

#### Telegram User IDs:
Для получения вашего Telegram User ID:
1. Найдите @userinfobot в Telegram
2. Отправьте ему `/start`
3. Скопируйте ваш User ID

## 🚀 Запуск

### Используя uv:

```bash
uv run python main.py
```

### Используя pip:

```bash
# Убедитесь, что виртуальное окружение активировано
python main.py
```

Если всё настроено правильно, вы увидите сообщение:
```
Starting Communication Coach Bot...
Authorized users: [123456789, 987654321]
Main admin: 123456789
Start polling
Run polling for bot @your_bot_name...
```

## 📱 Команды бота

### В групповых чатах:

- `/analyze_last_100` — анализ последних 100 сообщений
- `/analyze_last_24h` — анализ сообщений за последние 24 часа
- `/my_communication` — персональный анализ вашего стиля общения
- `/analyze_user @username` — анализ стиля общения указанного пользователя
- `/chat_stats` — показать статистику сообщений в чате

### В групповых чатах и личных сообщениях:

- `/analyze_user_all @username` — анализ стиля общения пользователя из всех чатов с ботом
- `/analyze_user_all <user_id>` — анализ по числовому ID пользователя

### Управление доступом (только для главного администратора):

- `/add_user @username` — добавить пользователя по имени
- `/add_user <user_id>` — добавить пользователя по ID
- `/remove_user @username` — удалить пользователя по имени
- `/remove_user <user_id>` — удалить пользователя по ID
- `/list_users` — показать список авторизованных пользователей
- `/clear_memory` — очистить старые сообщения из памяти для экономии RAM

### В личных сообщениях:

- `/start` — приветственное сообщение
- `/help` — справка по командам

## 🏗️ Структура проекта

```
telegram-communication-bot/
├── main.py              # Основной файл с логикой бота
├── config.py            # Конфигурация и переменные окружения
├── ai_analyzer.py       # Модуль для AI-анализа коммуникаций
├── message_cache.py     # Система кеширования сообщений
├── pyproject.toml       # Конфигурация проекта и зависимости
├── uv.lock             # Файл блокировки версий зависимостей
├── .env                # Переменные окружения (создается вами)
├── README.md           # Документация проекта
└── replit.md           # Техническая документация
```

## ⚙️ Конфигурация

Все настройки управляются через переменные окружения в файле `.env`:

| Переменная | Обязательная | По умолчанию | Описание |
|------------|--------------|--------------|----------|
| `TELEGRAM_BOT_TOKEN` | ✅ | - | Токен Telegram бота |
| `OPENAI_API_KEY` | ✅ | - | API ключ OpenAI |
| `AUTHORIZED_USERS` | ✅ | - | Список ID авторизованных пользователей через запятую |
| `CACHE_SIZE` | ❌ | 1000 | Максимальное количество сообщений в базе данных на чат |
| `MEMORY_CACHE_SIZE` | ❌ | 50 | Максимальное количество сообщений в памяти на чат |
| `RATE_LIMIT_SECONDS` | ❌ | 10 | Интервал между командами анализа в секундах |
| `LOG_LEVEL` | ❌ | INFO | Уровень логирования (DEBUG, INFO, WARNING, ERROR) |

## 🔒 Безопасность

- Бот хранит данные в SQLite базе данных с ограниченным кешем в памяти
- Анализы отправляются только в личные сообщения
- Доступ к командам ограничен авторизованными пользователями
- Первый пользователь в списке `AUTHORIZED_USERS` становится главным администратором
- Встроенная защита от спама с настраиваемыми лимитами

## 💾 Оптимизация памяти

Бот использует интеллектуальную систему кеширования для минимизации потребления оперативной памяти:

- **Двухуровневое кеширование**: Небольшой кеш в памяти (50 сообщений) + полная база данных
- **Автоматическая очистка**: Старые сообщения периодически удаляются из памяти
- **Настраиваемые лимиты**: Размер кеша можно настроить через переменные окружения
- **Команда очистки**: `/clear_memory` для ручной очистки памяти администратором
- **Мониторинг**: `/chat_stats` показывает статистику использования памяти

## 🐛 Отладка

### Проверка логов:

Бот выводит подробные логи. Для отладки установите:

```env
LOG_LEVEL=DEBUG
```

### Частые проблемы:

1. **Бот не отвечает**: Проверьте правильность `TELEGRAM_BOT_TOKEN`
2. **Ошибки AI анализа**: Проверьте `OPENAI_API_KEY` и баланс аккаунта
3. **Нет доступа к командам**: Убедитесь, что ваш User ID добавлен в `AUTHORIZED_USERS`
4. **Пустые анализы**: Бот анализирует только сообщения, отправленные после его добавления

### Тестирование:

1. Добавьте бота в тестовый групповой чат
2. Напишите несколько сообщений
3. Выполните `/chat_stats` для проверки кеширования
4. Попробуйте команду `/analyze_last_100`
5. Запуск всех тестов: `pytest -q`

## 📖 Дополнительная информация

- Бот использует модель GPT-4o для анализа коммуникаций
- Все данные обрабатываются конфиденциально
- Бот не видит историю сообщений до момента добавления в чат
- Анализ основан на принципах качественной обратной связи

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи бота
2. Убедитесь в правильности всех переменных окружения
3. Проверьте, что бот добавлен в чат с правильными разрешениями
4. Убедитесь, что у вас есть средства на аккаунте OpenAI

---

**Примечание**: Этот бот предназначен для анализа рабочих коммуникаций и должен использоваться в соответствии с политиками конфиденциальности вашей организации.